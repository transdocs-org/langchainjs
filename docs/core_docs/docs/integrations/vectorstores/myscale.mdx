---
sidebar_class_name: node-only
---

import CodeBlock from "@theme/CodeBlock";

# MyScale

:::tip 兼容性
仅支持 Node.js。
:::

[MyScale](https://myscale.com/) 是一个新兴的 AI 数据库，它将向量搜索和 SQL 分析的能力结合在一起，提供一种托管的、高效的、响应迅速的体验。

## 安装配置

1. 通过 [MyScale 的 Web 控制台](https://console.myscale.com/) 启动一个集群。更多信息请参见 [MyScale 官方文档](https://docs.myscale.com/en/quickstart/)。
2. 启动集群后，从集群的 `Actions` 菜单中查看您的 `连接详情（Connection Details）`。您将需要主机地址、端口、用户名和密码。
3. 在您的工作区中安装所需的 Node.js 对等依赖。

import IntegrationInstallTooltip from "@mdx_components/integration_install_tooltip.mdx";

<IntegrationInstallTooltip></IntegrationInstallTooltip>

```bash npm2yarn
npm install -S @langchain/openai @clickhouse/client @langchain/community @langchain/core
```

## 索引和查询文档

import InsertExample from "@examples/indexes/vector_stores/myscale_fromTexts.ts";

<CodeBlock language="typescript">{InsertExample}</CodeBlock>

## 从现有集合中查询文档

import SearchExample from "@examples/indexes/vector_stores/myscale_search.ts";

<CodeBlock language="typescript">{SearchExample}</CodeBlock>

## 相关内容

- 向量存储 [概念指南](/docs/concepts/#vectorstores)
- 向量存储 [操作指南](/docs/how_to/#vectorstores)